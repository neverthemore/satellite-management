package seminars.factory;

/**
 * Тип спутника. Используется параметрами создания (SatelliteParam) и
 * фабриками для самоидентификации (isSatelliteTypeSupported).
 */
public enum SatelliteType {
    IMAGE,
    COMMUNICATION
}
